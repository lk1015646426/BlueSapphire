# BlueSapphire 原生 UI 规范

版本：1.0（全量重构完成）

## 产品界面定位

BlueSapphire 是需要长期使用的 Windows 工作台。界面优先呈现当前任务、执行影响、风险与下一步，不使用展示型大标题或无业务意义的装饰区域。

## 信息架构

- 工作台：AI 对话和执行入口位于上半区；任务与最近活动位于下半区。
- 媒体工作台：素材筛选、批处理、检查和详情集中在一个高密度工作区。
- 清理工具：扫描、确认、执行与记录形成安全闭环。
- 更新日志：记录产品变化并支持受控编辑。
- 设置：统一管理外观、AI 服务和扩展连接。
- 左下固定区：更新日志、主题切换和设置。

AI 协作和任务中心不再作为独立页面，能力统一收纳在工作台中。

## 视觉系统

- 主题令牌由 `Themes/SharedTheme.xaml` 和 `App.xaml.cs` 提供，页面不得自行建立第二套调色板。
- 支持浅色、跟随系统和深色三种颜色模式。
- 支持默认暖陶、天蓝、钴蓝、石墨、泻湖、墨色、赭橙、棕褐八套完整主题。
- 使用 Segoe UI Variable；标题保持紧凑，数字和状态文字以清晰可读为先。
- 采用细边框、6–10px 圆角和平面层级，避免营销式 Hero、厚阴影和无意义粒子。
- 主色用于主要动作和当前状态；成功、复核、危险颜色只承担对应语义。

## 组件与状态

- 交互控件覆盖默认、悬停、焦点、按下和禁用状态。
- 危险操作必须先说明范围、影响和恢复方式，再允许确认。
- 空状态必须说明当前状态和可执行的下一步。
- 长任务展示阶段、进度、摘要和取消入口。
- AI 模型以小型实时标识显示，不占用正文空间。
- 对话框复用统一的工作台标题、分区、选择按钮和提示文字样式。

## 桌面适配与动效

- 主要内容不依赖横向滚动，紧凑窗口可收起导航文字。
- 适配 1280×800 与 1600×900，并支持小、标准、中、大四档字号。
- 动效只用于页面入场、卡片反馈和状态变化；开启“减少动态效果”后停用非必要动画。
- 所有主题的关键文字和主按钮满足至少 4.5:1 的对比度要求。

最终验收记录见 `UI_REDESIGN_AUDIT.md`。

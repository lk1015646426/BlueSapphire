<ContentDialog
    x:Class="BlueSapphire.DocumentConversionResultDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:BlueSapphire.Models"
    Title="处理结果"
    CloseButtonText="关闭"
    DefaultButton="Close">

    <ContentDialog.Resources>
        <DataTemplate x:Key="ConversionItemTemplate" x:DataType="models:DocumentConversionBatchItem">
            <Grid Padding="10" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border Width="34" Height="34" CornerRadius="17" Background="#12222222" VerticalAlignment="Top">
                    <FontIcon Glyph="{x:Bind StatusGlyph}" Foreground="{x:Bind StatusBrush}" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock Text="{x:Bind SourceName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{x:Bind DetailText}" Opacity="0.85" FontSize="12" TextWrapping="WrapWholeWords"/>
                    <TextBlock Text="{x:Bind PathText}" Opacity="0.55" FontSize="11" TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <TextBlock Grid.Column="2" Text="{x:Bind StatusText}" Foreground="{x:Bind StatusBrush}" VerticalAlignment="Top" FontWeight="SemiBold"/>

                <ToolTipService.ToolTip>
                    <ToolTip Content="{x:Bind ToolTipText}"/>
                </ToolTipService.ToolTip>
            </Grid>
        </DataTemplate>
    </ContentDialog.Resources>

    <StackPanel Spacing="12">
        <Border Background="#1200FFFF" BorderBrush="#3300FFFF" BorderThickness="1" CornerRadius="8" Padding="12">
            <StackPanel Spacing="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="OperationTextBlock" FontWeight="SemiBold"/>
                    <TextBlock x:Name="CreatedAtTextBlock" Grid.Column="1" Opacity="0.7" FontSize="12"/>
                </Grid>
                <TextBlock x:Name="SummaryTextBlock" FontWeight="SemiBold"/>
                <TextBlock Text="双击列表项可直接打开生成结果；失败或跳过项会打开源文件。" FontSize="12" Opacity="0.65"/>
            </StackPanel>
        </Border>

        <ListView x:Name="ResultList"
                  SelectionMode="None"
                  MaxHeight="520"
                  Width="560"
                  IsItemClickEnabled="False"
                  ItemTemplate="{StaticResource ConversionItemTemplate}"
                  DoubleTapped="ResultList_DoubleTapped"/>
    </StackPanel>
</ContentDialog>

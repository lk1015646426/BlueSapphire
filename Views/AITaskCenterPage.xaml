<Page
    x:Class="BlueSapphire.Views.AITaskCenterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:BlueSapphire.Models"
    Background="{StaticResource BgColor}">

    <Grid Padding="{StaticResource PagePaddingLarge}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Text="任务中心" Style="{StaticResource TextStyle_PageTitle}"/>
                <TextBlock Text="跨页面查看扫描、清理、媒体分析和 AI 工具任务；任务只会在完成或你主动取消时停止。"
                           Foreground="{StaticResource TextMuted}" TextWrapping="Wrap"/>
            </StackPanel>
            <Button Grid.Column="1"
                    Style="{StaticResource SubtleActionButtonStyle}"
                    Click="ClearCompleted_Click">
                <TextBlock Text="清除已结束任务"/>
            </Button>
        </Grid>

        <Grid Grid.Row="1">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="8"
                        Visibility="{x:Bind IsEmpty, Mode=OneWay}">
                <FontIcon Glyph="&#xE9F9;" FontSize="30" Foreground="{StaticResource TextMuted}"/>
                <TextBlock Text="当前没有任务" Foreground="{StaticResource TextMuted}"/>
            </StackPanel>

            <ListView ItemsSource="{x:Bind Tasks, Mode=OneWay}"
                      SelectionMode="None"
                      Visibility="{x:Bind HasTasks, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:AITaskRecord">
                        <Border Background="{StaticResource PanelSurface}"
                                BorderBrush="{StaticResource BorderSubtle}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="14,12"
                                Margin="0,0,0,8">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="5">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind Title}" FontSize="15" FontWeight="SemiBold"/>
                                        <TextBlock Text="{x:Bind StatusText}" Foreground="{StaticResource AccentInspect}"/>
                                        <TextBlock Text="{x:Bind ProgressText}" Foreground="{StaticResource TextMuted}"/>
                                    </StackPanel>
                                    <ProgressBar Value="{x:Bind Progress}" Maximum="100" Height="4"/>
                                    <TextBlock Text="{x:Bind Summary}" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{x:Bind TimelineSummaryText}"
                                               Foreground="{StaticResource TextMuted}"
                                               FontSize="11"
                                               FontFamily="Consolas"
                                               TextWrapping="Wrap"/>
                                    <TextBlock Text="{x:Bind UpdatedAtText}" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                </StackPanel>
                                <Button Grid.Column="1"
                                        Tag="{x:Bind Id}"
                                        Click="CancelTask_Click"
                                        VerticalAlignment="Top"
                                        Visibility="{x:Bind CanCancelNow, Converter={StaticResource BoolToVisibleConverter}}">
                                    <TextBlock Text="取消"/>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>

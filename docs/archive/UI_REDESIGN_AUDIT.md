# BlueSapphire UI 重构验收矩阵

本文件记录原生 Windows UI 全量重构的最终验收范围。网页原型仅作为设计基准，生产界面已使用 WinUI 3 重新实现。

## 页面与外壳

| 范围 | 状态 | 验收要点 |
|---|---|---|
| 应用外壳与导航 | 已完成 | 紧凑左侧导航；工作区入口在上，更新日志、主题切换和设置固定在左下 |
| 工作台 | 已完成 | AI 对话与执行入口合并；AI 区占上半区，任务与最近活动位于下半区 |
| 媒体工作台 | 已完成 | 紧凑工具栏、素材区、批处理与详情区统一使用语义主题令牌 |
| 清理工具 | 已完成 | 扫描、确认、结果和记录流程完整；磁盘选择使用纵向紧凑列表，无横向裁切 |
| 设置 | 已完成 | 主题、颜色模式、字号、减少动态效果、AI 服务和扩展集中配置 |
| 更新日志 | 已完成 | 紧凑列表和详情层级；新增记录使用统一工作台对话框 |

独立的 AI 协作页、AI 任务中心及其工具入口已删除，能力已合并到工作台。

## 对话框与控件

| 范围 | 状态 |
|---|---|
| 重命名预览 | 已完成 |
| 重复文件结果 | 已完成 |
| 格式转换 | 已完成 |
| 自动增强 | 已完成 |
| 高级图片编辑 | 已完成 |
| 技能网络配置 | 已完成 |
| 更新日志输入 | 已完成 |

所有对话框复用 `SharedTheme.xaml` 的工作台式标题、分区、选择按钮、提示文字和开关样式。

## 设计系统

- 颜色模式：浅色、跟随系统、深色。
- 主题配色：默认暖陶、天蓝、钴蓝、石墨、泻湖、墨色、赭橙、棕褐；每套均覆盖页面、面板、侧栏、边框、文字、主色和焦点色。
- 字号：小、标准、中、大；已验证大字号下的工作台和设置页布局。
- 动效：保留克制的页面、卡片和状态反馈，并提供“减少动态效果”开关。
- 排版与空间：取消营销式大标题，统一紧凑标题和高密度工作区；模型名称使用小型实时状态标识。
- 可访问性：主题主按钮前景与背景对比度均不低于 4.5:1；交互控件保留键盘焦点和自动化名称。

## 验证记录

- 自动化回归：233/233 通过，0 失败。
- x64 Debug 无增量构建：0 警告、0 错误。
- x64 Release 无增量构建：0 警告、0 错误。
- 运行时导航：工作台、媒体工作台、清理工具、更新日志、设置均通过 `--tool=` 直达并获得非零句柄，`Responding=True`。
- 视觉走查：覆盖 1600×900、1280×800、900×700；覆盖 Dark/Light、Default/Sky/Ochre/Sepia、标准/大字号和减少动态效果。
- 截图复核后修复：窗口最小宽度、媒体批处理横向可达性、工作台窄屏次要区、Cleaner 高缩放主操作、设置主题网格和 AI 顶栏溢出。
- 对话框：3 项契约测试验证统一资源、主/取消动作、危险范围说明和自动化名称；各切片均完成真实 XAML 编译。
- 历史保护：`Assets/DevMatrixLog.json` 测试前后 SHA-256 均为 `A9460CC2C03CFED65B36BE74E91D5CD2FBACD978D4243F8E6EFB9C81EBB64FEC`。
- 本轮未对真实用户媒体执行格式转换、增强、覆盖或删除。

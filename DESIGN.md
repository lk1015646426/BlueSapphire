# BlueSapphire UI 设计协定（历史归档）

> 此文件属于重构前的设计协定。新的 UI 设计目标和验收标准以 [UI_REDESIGN_SPEC.md](UI_REDESIGN_SPEC.md) 为准。

版本：0.1  
用途：作为 BlueSapphire 后续 UI 设计、审计和重设计的项目级约定。

## 外部参考

- Penpot：<https://github.com/penpot/penpot> / <https://penpot.app/>
  - 用于参考设计系统、组件化、原型验证、信息层级和页面节奏。
  - 不直接复制 Penpot 的营销页面或视觉皮肤。
- Hallmark：<https://github.com/Nutlope/hallmark>
  - 用于反模板化设计、页面结构审计、参考设计 DNA 提取和重设计流程。
  - Hallmark 的 Web 规则需要转换为 WinUI 3 的窗口、DPI、键盘、触摸、高对比度和无障碍规则。

## 必须遵循的设计关系

BlueSapphire 的 UI 需要表达“主人—管家—佣人”的关系：

- 用户是最终决策者，界面必须清楚表达目标、影响和确认点。
- AI 中控是全局管家，应该有明确的工作状态、计划、正在调用的工具和结果汇总。
- 媒体工作台、清理工具及未来工具是专业执行者，各自拥有自己的页面节奏和信息结构。
- 应用外壳、导航、主题令牌和交互状态保持一致；工具页面不强制使用同一种卡片布局。

## 设计原则

1. 先确定页面指纹，再确定颜色。每个页面必须先明确宏观结构、内容密度、主要动作和状态节奏，不能只在旧页面上换颜色。
2. 共享令牌，允许结构变化。颜色、字体、间距、焦点态、危险态和动效使用主题令牌；主页、AI、媒体、清理和设置允许使用不同的页面级组合。
3. 有活力但不喧闹。可以使用更明亮的表面层、领域色、插图、渐变和状态动效，但不能依赖霓虹堆叠、厚阴影或无意义粒子。
4. 内容优先于装饰。背景图形必须服务于层级或状态；空状态必须告诉用户下一步；每个主要页面首屏必须有明确的开始动作。
5. 真实状态优先。不得编造指标、成功率、用户数量或任务结果。没有数据时使用明确的空状态或待确认标记。
6. 危险操作先解释后确认。清理、移动、删除、外部调用等操作必须展示范围、影响、风险、恢复方式和确认状态。
7. 组件必须覆盖完整状态。交互控件至少考虑默认、悬停、焦点、按下、禁用、加载、错误和成功；WinUI 页面还要验证键盘导航、系统焦点、高对比度和缩放。
8. 不做像素复制。研究 Penpot 或其他参考时只提取结构、节奏、字体角色、色彩锚点和交互原则，使用 BlueSapphire 自己的内容和资产。

## 每次 UI 设计工作的固定流程

1. 读取本文件、`DESIGN_LANGUAGE.md`、`Themes/SharedTheme.xaml` 和目标页面。
2. 使用 Hallmark 的 `audit` 思路先列出当前页面的结构重复、信息层级、状态和反模式问题。
3. 如果有参考项目或截图，使用 Hallmark 的 `study` 思路提取 DNA：宏观结构、页面 archetype、字体角色、色彩锚点和密度；不复制像素。
4. 使用 Penpot 思路先确定组件、变量和原型状态，再修改 XAML/C#。
5. 优先改动主题令牌和页面组合，避免在单个控件中散落颜色、字体和间距常量。
6. 修改后验证至少一个完整用户流程，以及窗口缩放、键盘焦点、深色/浅色主题和高对比度状态。
7. 输出设计变更时说明：保留了什么、改变了什么、参考了什么、哪些部分是 BlueSapphire 自己的设计。

## 当前状态

当前深色蓝宝石主题属于旧版视觉基线，不是不可改变的最终风格。后续重设计可以保留品牌识别和 Windows 工具属性，同时重新评估背景亮度、表面层次、领域色、页面结构和 AI 中控的活力。
